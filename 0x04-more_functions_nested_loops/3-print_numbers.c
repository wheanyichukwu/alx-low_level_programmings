#include "main.h"

/**
 * print_numbers - Print the numbers since 0 up to 9
 * Return: return nothing
 */

void print_numbers(void)

{
	int n;

	for (n = 48; x < 58; n++)
	{
	putchar(n);
	}
	putchar(10);
}
